ctf.show_web4 (2026/07/03)
- 文件包含题，但源码展示的只是highlight_string截取的代码片段，真正的过滤逻辑在前面
- 过滤: stripos($url,"php")===false && stripos($url,"data")===false → 含php或data就die
- 绕过方式: 日志投毒RCE（User-Agent写马→包含access.log执行）
- flag位置: /var/www/flag.txt（不在根目录也不在web根）
- 教训: 页面展示的源码不一定是完整的，要读真实源码